@page
@model FurnitureApp.Pages.Admin.CartItems.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "_LayoutAdmin";
    ViewData["CurrentTab"] = "cartitems";
}

<h1>Details</h1>

<div>
    <h4>CartItem</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CartItem.Quantity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CartItem.Quantity)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CartItem.Selected)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CartItem.Selected)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CartItem.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
