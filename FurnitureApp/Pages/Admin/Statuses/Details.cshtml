@page
@model FurnitureApp.Pages.Admin.Statuses.DetailsModel

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Details";
    ViewData["CurrentTab"] = "status";
}

<h1>Details</h1>

<div>
    <h4>Status</h4>
    <hr />
    <dl class="row d-flex flex-column">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Status.StatusOrder)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Status.StatusOrder)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Status.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
