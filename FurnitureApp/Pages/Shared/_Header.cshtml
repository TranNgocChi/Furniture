@model _HeaderModel.HeaderModel

@{
	var headerJson = ViewData["Header"] as _HeaderModel.HeaderModel;
}

<!-- Start Header/Navigation -->
<nav class="custom-navbar navbar navbar navbar-expand-md navbar-dark bg-dark" arial-label="Furni navigation bar">

	<div class="container">
		<a class="navbar-brand" href="index">Furni<span>.</span></a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsFurni" aria-controls="navbarsFurni" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarsFurni">
			<ul class="custom-navbar-nav navbar-nav ms-auto mb-2 mb-md-0">
				<li class="nav-item @(Model.CurrentTab == "home" ? "active":"")">
					<a class="nav-link" asp-page="/Index">Home</a>
				</li>
				<li class="nav-item @(Model.CurrentTab == "shop" ? "active":"")">
					<a class="nav-link" asp-page="/Shop/Index" asp-route-headerJson="@headerJson">Shop</a>
				</li>
				<li class="nav-item @(Model.CurrentTab == "about-us" ? "active":"")">
					<a class="nav-link" href="about.html">About us</a>
				</li>
				<li class="nav-item @(Model.CurrentTab == "service" ? "active":"")">
					<a class="nav-link" href="services.html">Services</a>
				</li>
				<li class="nav-item @(Model.CurrentTab == "blog" ? "active":"")">
					<a class="nav-link" href="blog.html">Blog</a>
				</li>
				<li class="nav-item @(Model.CurrentTab == "contact" ? "active":"")">
					<a class="nav-link" href="contact.html">Contact us</a>
				</li>
			</ul>

			<ul class="custom-navbar-cta navbar-nav mb-2 mb-md-0 ms-5">
				<li class="@(Model.CurrentTab == "cart" ? "border border-4 border-warning rounded-circle":"")"><a class="nav-link" asp-page="/Cart" asp-route-headerJson="@headerJson"><img src="~/images/cart.svg"></a></li>
				@if (String.IsNullOrEmpty(Model.UserName))
				{
					<li><a class="nav-link" href="Login"><img src="~/images/user.svg"></a></li>
				}
				else
				{
					<li><a class="nav-link" href="Login">@Model.UserName</a></li>
				}
		</div>
	</div>

</nav>
<!-- End Header/Navigation -->

<!-- Start Hero Section -->
<div class="hero">
	<div class="container">
		<div class="row justify-content-between">
			<div class="col-lg-5">
				<div class="intro-excerpt">
					<h1>Modern Interior <span clsas="d-block">Design Studio</span></h1>
					<p class="mb-4">Donec vitae odio quis nisl dapibus malesuada. Nullam ac aliquet velit. Aliquam vulputate velit imperdiet dolor tempor tristique.</p>
					<p><a href="" class="btn btn-secondary me-2">Shop Now</a><a href="#" class="btn btn-white-outline">Explore</a></p>
				</div>
			</div>
			<div class="col-lg-7">
				<div class="hero-img-wrap">
					<img src="~/images/leather_sofa.jpg" class="img-fluid">
				</div>
			</div>
		</div>
	</div>
</div>
<!-- End Hero Section -->
